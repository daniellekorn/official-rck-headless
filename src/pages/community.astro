---
import Layout from "../layouts/Layout.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import PageHeader from "../components/PageHeader.astro";
import ContactButtons from "../components/ContactButtons.astro";
import Highlight from "../components/Highlight.astro";
import { getCommunityPage, getCommunityMembers } from "../lib/community";
import { ricosToPlainText } from "../lib/ricos";
import { googleMapsUrl } from "../lib/maps";

const page = await getCommunityPage();
const members = await getCommunityMembers();
const hasMembers = members.length > 0;

// Member Spotlight (the family-carousel section below, id="members" — what
// Yosef calls "Community Spotlight") is pulled off the live site for now but
// kept in full so it can come back with a one-line flip. Set true to
// republish; it will render below the new "Community Voices" section, which
// permanently owns this slot on the page and took over the video tile that
// used to open the Member Spotlight carousel (see design-log #060 for the
// same flag pattern used on Torah Sheets' ParshaLink).
const SHOW_MEMBER_SPOTLIGHT = false;

const mealsDescription = page?.mealsDescription ? ricosToPlainText(page.mealsDescription) : "";

// Contact page link with a prefilled, locked subject line (contact.astro reads
// ?subject= and shows it read-only) — used for the inline name links below.
const contactHref = (subject: string) => `/contact?subject=${encodeURIComponent(subject)}`;

const BEIS_DIN_SERVICES = [
	"Monetary Disputes",
	"Arbitration & Mediation",
	"Halachic Wills",
	"Shalom Bayis",
	"Halachic Contracts",
];

// Meals hosting contact — shown while the CMS `mealsFamilyName` field (a
// single-family setup, see below) is unpopulated. Number is Israeli mobile,
// entered in local format; <ContactButtons> derives the `tel:` and WhatsApp
// (`wa.me`, country code 972) links from that same number.
const MEALS_CONTACT = { name: "Yael Bernstein", phone: "053-216-4726" };

// Aliya FAQ contact — same hardcoded-fallback pattern as MEALS_CONTACT above
// (no CMS field for this yet; promote to CommunityPage if it ever needs to
// be office-editable without a code change).
const ALIYA_CONTACT = { name: "Rabbi Isaac Bernstein", phone: "053-216-4727" };

// Second meals contact, offered alongside MEALS_CONTACT — same hardcoded
// pattern, shown right under Yael Bernstein's line.
const MEALS_CONTACT_2 = { name: "Rabbi Yisroel Zaslow", phone: "053-347-8419" };

// Every rabbi named on this page links to his bio on /team, which has two
// sections (`#leadership` / `#avreichim`, see src/lib/team.ts ROLE_GROUPS) —
// there's no per-person anchor, so the link scrolls to that rabbi's section.
// Hardcoded here to whichever section each named rabbi's TeamMembers row is
// actually in; keep in sync if a role changes.
const TEAM_ANCHOR = {
	leadership: "/team#leadership",
	avreichim: "/team#avreichim",
};
const rabbiLinkClass = "font-medium text-gold-600 transition-colors hover:text-gold-500";

// Accordion chrome shared by all five FAQ topics — native <details>/<summary>
// so each opens/closes independently with zero JS and keyboard support for
// free (see design log #047 for why this beats a custom aria-expanded toggle
// here, unlike the team page's hover+pin card).
const detailsClass = "group border-b border-navy-100 last:border-b-0 scroll-mt-28";
const summaryClass =
	"flex cursor-pointer list-none items-center justify-between gap-4 py-6 text-left [&::-webkit-details-marker]:hidden";
const questionClass = "font-display text-xl sm:text-2xl font-medium text-navy-600 pr-4";
const chevronClass = "h-5 w-5 shrink-0 text-gold-500 transition-transform duration-300 group-open:rotate-180";
const panelClass = "pb-8 pt-1 text-navy-500 leading-relaxed";
---

<Layout>
	<Nav />
	<main>
		<PageHeader titleNavy="Welcome to Our Community" />

		<!-- FAQ topics -->
		<section class="bg-white py-4 sm:py-6">
			<div class="container-page">
				<div class="mx-auto max-w-3xl">

					<!-- 1. Meals -->
					<details id="meals" class={detailsClass}>
						<summary class={summaryClass}>
							<span class={questionClass}>New to the community? Let us set you up with meals</span>
							<svg viewBox="0 0 24 24" class={chevronClass} fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true">
								<path d="M6 9l6 6 6-6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</summary>
						<div class={panelClass}>
							{page?.mealsFamilyName ? (
								<div class="flex flex-col gap-6 sm:flex-row sm:items-start">
									{page.mealsPhotoUrl && (
										<img
											src={page.mealsPhotoUrl}
											alt={page.mealsFamilyName}
											loading="lazy"
											class="h-40 w-40 shrink-0 rounded-md object-cover ring-1 ring-navy-100"
										/>
									)}
									<div class="min-w-0">
										<p class="font-medium text-gold-600">{page.mealsFamilyName}</p>
										<div class="mt-1.5 flex flex-wrap items-center gap-x-5 gap-y-1 text-sm">
											{page.mealsPhone && (
												<ContactButtons phone={page.mealsPhone} name={page.mealsFamilyName} />
											)}
											{page.mealsEmail && (
												<a href={`mailto:${page.mealsEmail}`} class="font-medium text-gold-600 hover:text-gold-500 break-all">
													{page.mealsEmail}
												</a>
											)}
										</div>
										{mealsDescription && <p class="mt-3 whitespace-pre-line">{mealsDescription}</p>}
									</div>
								</div>
							) : (
								<div>
									<p>We&rsquo;d love to set you up with local families for Shabbos and Yom Tov meals!</p>
									<div class="mt-3 flex flex-wrap items-center gap-2">
										<span>
											Reach out to <span class="font-medium text-gold-600">{MEALS_CONTACT.name}</span>
										</span>
										<ContactButtons phone={MEALS_CONTACT.phone} name={MEALS_CONTACT.name} />
									</div>
									<div class="mt-3 flex flex-wrap items-center gap-2">
										<span>
											Or <a href={TEAM_ANCHOR.avreichim} class={rabbiLinkClass}>{MEALS_CONTACT_2.name}</a>
										</span>
										<ContactButtons phone={MEALS_CONTACT_2.phone} name={MEALS_CONTACT_2.name} />
									</div>
								</div>
							)}
						</div>
					</details>

					<!-- 2. Aliya -->
					<details id="aliya" class={detailsClass}>
						<summary class={summaryClass}>
							<span class={questionClass}>Just made Aliya? Questions?</span>
							<svg viewBox="0 0 24 24" class={chevronClass} fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true">
								<path d="M6 9l6 6 6-6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</summary>
						<div class={panelClass}>
							<p>Welcome to Eretz Yisroel and kol hakavod on making the move!</p>
							<p class="mt-3">
								Many members of our team have gone through the process of aliya themselves and are
								more than happy to be of assistance. We would love to help!
							</p>
							<p class="mt-3">
								Contact <a href={TEAM_ANCHOR.avreichim} class={rabbiLinkClass}>{ALIYA_CONTACT.name}</a> with any questions.
							</p>
							<ContactButtons phone={ALIYA_CONTACT.phone} name={ALIYA_CONTACT.name} class="mt-3" />
							<p class="mt-4">B&rsquo;Hatzlacha and a Yishuv Tov!</p>
						</div>
					</details>

					<!-- 3. Gabbai -->
					<details id="gabbai" class={detailsClass}>
						<summary class={summaryClass}>
							<span class={questionClass}>Yahrtzeit? Special Simcha? Contact our gabbai.</span>
							<svg viewBox="0 0 24 24" class={chevronClass} fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true">
								<path d="M6 9l6 6 6-6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</summary>
						<div class={panelClass}>
							<div class="flex flex-col gap-6 sm:flex-row sm:items-start">
								{page?.gabbaiPhotoUrl && (
									<img
										src={page.gabbaiPhotoUrl}
										alt={page.gabbaiName ?? "Gabbai"}
										loading="lazy"
										class="h-40 w-40 shrink-0 rounded-md object-cover ring-1 ring-navy-100"
									/>
								)}
								<div class="min-w-0">
									{page?.gabbaiName ? (
										<p>
											For Shabbos, contact our gabbai,{" "}
											<a href={contactHref("Re: Gabbi")} class="font-medium text-gold-600 hover:text-gold-500">
												{page.gabbaiName}
											</a>
											, regarding yahrtzeits, special simchas, and other gabbai matters.
											{page.gabbaiWifeName && (
												<>
													{" "}His wife,{" "}
													<a href={contactHref("Re: Tefillat Yeladim")} class="font-medium text-gold-600 hover:text-gold-500">
														{page.gabbaiWifeName}
													</a>
													, also leads our popular and exciting Tefillat Yeladim!
												</>
											)}
										</p>
									) : (
										<p>Gabbai contact info is coming soon — check back shortly, or reach out via our <a href="/contact" class="font-medium text-gold-600 hover:text-gold-500">contact page</a>.</p>
									)}
									<div class="mt-3 flex flex-wrap items-center gap-x-5 gap-y-1 text-sm">
										{page?.gabbaiPhone && (
											<ContactButtons phone={page.gabbaiPhone} name={page.gabbaiName ?? "the gabbai"} />
										)}
										{page?.gabbaiEmail && (
											<a href={`mailto:${page.gabbaiEmail}`} class="font-medium text-gold-600 hover:text-gold-500 break-all">
												{page.gabbaiEmail}
											</a>
										)}
									</div>
									<a
										href="/daven#shabbos"
										class="mt-4 inline-flex items-center gap-1.5 font-sans text-sm font-bold uppercase tracking-[0.14em] text-gold-600 hover:text-gold-500"
									>
										View Shabbos Times
										<svg viewBox="0 0 24 24" class="h-3.5 w-3.5" fill="none" stroke="currentColor" stroke-width="2.4" aria-hidden="true">
											<path d="M9 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round" />
										</svg>
									</a>
								</div>
							</div>
						</div>
					</details>

					<!-- 4. Taharas HaMishpacha -->
					<details id="taharas" class={detailsClass}>
						<summary class={summaryClass}>
							<span class={questionClass}>Taharas HaMishpacha</span>
							<svg viewBox="0 0 24 24" class={chevronClass} fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true">
								<path d="M6 9l6 6 6-6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</summary>
						<div class={panelClass}>
							<p class="font-sans text-sm font-bold uppercase tracking-[0.18em] text-navy-400">
								Need to ask or drop off a shaalah? Contact our rabbonim:
							</p>
							<div class="mt-5 grid gap-8 sm:grid-cols-2">
								<div>
									<p class="font-sans text-xs font-bold uppercase tracking-[0.2em] text-gold-500">English</p>
									{page?.taharasEnglishRabbiName && (
										<a href={TEAM_ANCHOR.leadership} class="mt-1.5 inline-block text-lg font-medium text-navy-600 hover:text-gold-600">
											{page.taharasEnglishRabbiName}
										</a>
									)}
									{page?.taharasEnglishRabbiPhone && (
										<ContactButtons
											phone={page.taharasEnglishRabbiPhone}
											name={page.taharasEnglishRabbiName ?? "the rabbi"}
											class="mt-1.5"
										/>
									)}
									{page?.taharasEnglishRabbiAddress && (
										<address class="mt-1 flex items-start gap-1.5 not-italic text-sm text-navy-400">
											<svg viewBox="0 0 24 24" class="mt-0.5 h-3.5 w-3.5 shrink-0 text-gold-500" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
												<path d="M12 21s-8-7.3-8-12a8 8 0 1 1 16 0c0 4.7-8 12-8 12Z" stroke-linecap="round" stroke-linejoin="round" />
												<circle cx="12" cy="9" r="2" />
											</svg>
											<span class="flex flex-wrap items-baseline gap-x-2">
												<a href={googleMapsUrl(page.taharasEnglishRabbiAddress)} target="_blank" rel="noopener noreferrer" class="hover:text-gold-600 hover:underline">
													{page.taharasEnglishRabbiAddress}
												</a>
											</span>
										</address>
									)}
									{page?.taharasEnglishRabbi2Name && (
										<div class="mt-5">
											{page.taharasEnglishRabbi2Name && (
												<a href={TEAM_ANCHOR.avreichim} class="inline-block text-lg font-medium text-navy-600 hover:text-gold-600">
													{page.taharasEnglishRabbi2Name}
												</a>
											)}
											{page?.taharasEnglishRabbi2Phone && (
												<ContactButtons
													phone={page.taharasEnglishRabbi2Phone}
													name={page.taharasEnglishRabbi2Name ?? "the rabbi"}
													class="mt-1.5"
												/>
											)}
											{page?.taharasEnglishRabbi2Address && (
												<address class="mt-1 flex items-start gap-1.5 not-italic text-sm text-navy-400">
													<svg viewBox="0 0 24 24" class="mt-0.5 h-3.5 w-3.5 shrink-0 text-gold-500" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
														<path d="M12 21s-8-7.3-8-12a8 8 0 1 1 16 0c0 4.7-8 12-8 12Z" stroke-linecap="round" stroke-linejoin="round" />
														<circle cx="12" cy="9" r="2" />
													</svg>
													<span class="flex flex-wrap items-baseline gap-x-2">
														<a href={googleMapsUrl(page.taharasEnglishRabbi2Address)} target="_blank" rel="noopener noreferrer" class="hover:text-gold-600 hover:underline">
															{page.taharasEnglishRabbi2Address}
														</a>
													</span>
												</address>
											)}
										</div>
									)}
								</div>
								<div>
									<p class="font-sans text-xs font-bold uppercase tracking-[0.2em] text-gold-500">Hebrew</p>
									{page?.taharasHebrewRabbiName && (
										<a href={TEAM_ANCHOR.leadership} class="mt-1.5 inline-block text-lg font-medium text-navy-600 hover:text-gold-600">
											{page.taharasHebrewRabbiName}
										</a>
									)}
									{page?.taharasHebrewRabbiPhone && (
										<ContactButtons
											phone={page.taharasHebrewRabbiPhone}
											name={page.taharasHebrewRabbiName ?? "the rabbi"}
											class="mt-1.5"
										/>
									)}
									{page?.taharasHebrewRabbiAddress && (
										<address class="mt-1 flex items-start gap-1.5 not-italic text-sm text-navy-400">
											<svg viewBox="0 0 24 24" class="mt-0.5 h-3.5 w-3.5 shrink-0 text-gold-500" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
												<path d="M12 21s-8-7.3-8-12a8 8 0 1 1 16 0c0 4.7-8 12-8 12Z" stroke-linecap="round" stroke-linejoin="round" />
												<circle cx="12" cy="9" r="2" />
											</svg>
											<span class="flex flex-wrap items-baseline gap-x-2">
												<a href={googleMapsUrl(page.taharasHebrewRabbiAddress)} target="_blank" rel="noopener noreferrer" class="hover:text-gold-600 hover:underline">
													{page.taharasHebrewRabbiAddress}
												</a>
											</span>
										</address>
									)}
								</div>
							</div>
						</div>
					</details>

					<!-- 5. Beis Din -->
					<details id="beis-din" class={detailsClass}>
						<summary class={summaryClass}>
							<span class={questionClass}>Our Beis Din and Dayanim</span>
							<svg viewBox="0 0 24 24" class={chevronClass} fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true">
								<path d="M6 9l6 6 6-6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</summary>
						<div class={panelClass}>
							{page?.beisDinContactName ? (
								<>
									<p>
										At RCK we have a professional and well-trained team of dayanim ready to serve the
										community with sensitivity, professionalism, and full halachic integrity. Contact{" "}
										<a href={TEAM_ANCHOR.leadership} class="font-medium text-gold-600 hover:text-gold-500">{page.beisDinContactName}</a>{" "}
										to schedule an appointment.
									</p>
									{page.beisDinContactPhone && (
										<ContactButtons phone={page.beisDinContactPhone} name={page.beisDinContactName} class="mt-3" />
									)}
								</>
							) : (
								<p>
									At RCK we have a professional and well-trained team of dayanim ready to serve the
									community with sensitivity, professionalism, and full halachic integrity.
								</p>
							)}

							<div class="mt-5">
								<p class="font-sans text-xs font-bold uppercase tracking-[0.2em] text-gold-500">Our Services</p>
								<ul class="mt-2.5 space-y-1.5">
									{BEIS_DIN_SERVICES.map((service) => (
										<li class="flex items-center gap-2">
											<svg viewBox="0 0 24 24" class="h-4 w-4 shrink-0 text-gold-500" fill="none" stroke="currentColor" stroke-width="2.4" aria-hidden="true">
												<path d="M5 12.5l4.5 4.5L19 7" stroke-linecap="round" stroke-linejoin="round" />
											</svg>
											<span>{service}</span>
										</li>
									))}
								</ul>
							</div>

							{page?.beisDinContactEmail && (
								<div class="mt-5 flex flex-wrap gap-x-5 gap-y-1 text-sm">
									<a href={`mailto:${page.beisDinContactEmail}`} class="font-medium text-gold-600 hover:text-gold-500 break-all">
										{page.beisDinContactEmail}
									</a>
								</div>
							)}
						</div>
					</details>

				</div>
			</div>
		</section>

		{/* Community Voices: text-left / video-right band, same two-column
		    shape as the Learn page's CoverflowCarousel "with-aside" layout
		    (hand-rolled here rather than reusing that component, since it's
		    one fixed video, not a flyer carousel to page through). Sits in
		    the slot the Member Spotlight section used to occupy; that
		    section (below, currently flagged off) will render underneath
		    this one whenever SHOW_MEMBER_SPOTLIGHT flips back on. */}
		<section id="community-voices" class="bg-mist py-14 sm:py-16">
			<div class="container-page grid grid-cols-1 justify-items-center items-center gap-10 lg:gap-12 lg:grid-cols-[minmax(0,440px)_minmax(0,1fr)] lg:justify-items-stretch">
				<div class="mx-auto w-full max-w-sm lg:mx-0">
					<h2
						class="sm:whitespace-nowrap text-center lg:text-left font-display text-[2.25rem] sm:text-[2.75rem] lg:text-[3rem] leading-[1.05] font-medium uppercase text-navy-600"
						data-reveal="left"
					>
						Community Voices
					</h2>
					<p class="mt-5 text-center lg:text-left text-lg leading-relaxed text-navy-500" data-reveal="up" data-reveal-delay="180">
						<Highlight>Hear what RCK means to our community,</Highlight> in their own words.{" "}
						<Highlight>Watch the video</Highlight> from last year&rsquo;s dinner.
					</p>
				</div>

				<div class="min-w-0 w-full mx-auto max-w-2xl" data-reveal="right">
					<div class="group relative aspect-video w-full overflow-hidden rounded-md ring-1 ring-navy-100 shadow-[0_25px_60px_-18px_rgba(7,23,58,0.35)] bg-navy-700" data-video-spotlight data-video-id="TUAYUbxZCHY">
						<img
							data-video-spotlight-thumb
							src="https://img.youtube.com/vi/TUAYUbxZCHY/hqdefault.jpg"
							alt="RCK Community Voices – What the RCK Means to Us"
							loading="lazy"
							class="h-full w-full object-cover opacity-75 transition-opacity duration-300 group-hover:opacity-55"
						/>
						<div data-video-spotlight-scrim class="absolute inset-0 bg-navy-700/30" aria-hidden="true"></div>
						<button
							type="button"
							data-video-spotlight-play
							aria-label="Play video"
							class="absolute inset-0 flex items-center justify-center focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gold-500"
						>
							<span class="flex h-16 w-16 sm:h-20 sm:w-20 items-center justify-center rounded-full bg-gold-500 text-navy-700 shadow-lg transition-transform duration-300 group-hover:scale-110">
								<svg viewBox="0 0 24 24" class="h-7 w-7 sm:h-8 sm:w-8 translate-x-0.5" fill="currentColor" aria-hidden="true">
									<path d="M8 5v14l11-7z" />
								</svg>
							</span>
						</button>
						<button
							type="button"
							data-lightbox-open
							data-lightbox-gallery={JSON.stringify([{ type: "youtube", videoId: "TUAYUbxZCHY" }])}
							data-lightbox-index="0"
							data-lightbox-title="RCK Community Voices – What the RCK Means to Us"
							aria-label="Enlarge video"
							class="absolute top-3 right-3 z-10 flex h-9 w-9 items-center justify-center rounded-sm bg-navy-700/70 text-white backdrop-blur-sm transition-colors hover:bg-navy-700/90 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gold-500"
						>
							<svg viewBox="0 0 24 24" class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
								<path d="M9 3H3v6M15 3h6v6M3 15v6h6M21 15v6h-6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</button>
					</div>
				</div>
			</div>
		</section>

		{SHOW_MEMBER_SPOTLIGHT && (
		<section id="members" class="scroll-mt-24 bg-mist py-16 sm:py-20 md:py-24">
			<div class="container-page">
				<div class="text-center mb-10 sm:mb-14" data-reveal="up">
					<h2 class="font-display text-3xl sm:text-4xl md:text-[2.75rem] leading-[1.05] font-medium text-navy-600">
						Member Spotlight
					</h2>
					<span class="gold-rule mx-auto mt-4" aria-hidden="true" />
					<p class="mx-auto mt-5 max-w-xl text-base sm:text-lg text-navy-500 leading-relaxed">
						Meet some of the people who make up our community!
					</p>
				</div>

				{!hasMembers && (
					<div class="text-center">
						<p class="font-sans text-sm font-bold uppercase tracking-[0.18em] text-gold-500">Coming soon</p>
						<p class="mx-auto mt-4 max-w-xl text-base text-navy-500 leading-relaxed">
							Once the office adds families in the Wix CMS dashboard
							(<code class="rounded bg-white px-1.5 py-0.5 text-sm">CommunityMembers</code> collection),
							they'll appear here automatically.
						</p>
					</div>
				)}

				{hasMembers && (
					<div class="relative" data-members-carousel>
						<div
							class="members-track flex gap-5 sm:gap-6 overflow-x-auto scroll-smooth snap-x snap-mandatory px-1 pb-2"
							data-members-track
						>
						{/* Fixed video spotlight — always first, styled distinctly (navy +
						    play button) from the CMS-driven family cards below, and sized
						    slightly larger since it anchors the carousel. Not CMS-driven:
						    it's a one-off video, not a repeating family entry. Play button
						    swaps the thumbnail for an inline autoplaying embed; the separate
						    corner button opens the same video full-size in the site lightbox. */}
						<div class="w-[260px] sm:w-[300px] md:w-[320px] shrink-0 snap-start">
							<div class="video-spotlight-card flex flex-col h-full w-full rounded-md overflow-hidden ring-1 ring-navy-100 shadow-[0_12px_30px_-20px_rgba(15,23,42,0.25)] bg-navy-700 transition-all duration-300 hover:-translate-y-1 hover:shadow-[0_22px_45px_-22px_rgba(15,23,42,0.45)]">
								<div class="group relative aspect-square w-full overflow-hidden" data-video-spotlight data-video-id="TUAYUbxZCHY">
									<img
										data-video-spotlight-thumb
										src="https://img.youtube.com/vi/TUAYUbxZCHY/hqdefault.jpg"
										alt="RCK Community Voices – What the RCK Means to Us"
										loading="lazy"
										class="h-full w-full object-cover opacity-75 transition-opacity duration-300 group-hover:opacity-55"
									/>
									<div data-video-spotlight-scrim class="absolute inset-0 bg-navy-700/30" aria-hidden="true"></div>
									<button
										type="button"
										data-video-spotlight-play
										aria-label="Play video"
										class="absolute inset-0 flex items-center justify-center focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gold-500"
									>
										<span class="flex h-14 w-14 items-center justify-center rounded-full bg-gold-500 text-navy-700 shadow-lg transition-transform duration-300 group-hover:scale-110">
											<svg viewBox="0 0 24 24" class="h-6 w-6 translate-x-0.5" fill="currentColor" aria-hidden="true">
												<path d="M8 5v14l11-7z" />
											</svg>
										</span>
									</button>
									<button
										type="button"
										data-lightbox-open
										data-lightbox-gallery={JSON.stringify([{ type: "youtube", videoId: "TUAYUbxZCHY" }])}
										data-lightbox-index="0"
										data-lightbox-title="RCK Community Voices – What the RCK Means to Us"
										aria-label="Enlarge video"
										class="absolute top-2 right-2 z-10 flex h-8 w-8 items-center justify-center rounded-sm bg-navy-700/70 text-white backdrop-blur-sm transition-colors hover:bg-navy-700/90 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gold-500"
									>
										<svg viewBox="0 0 24 24" class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
											<path d="M9 3H3v6M15 3h6v6M3 15v6h6M21 15v6h-6" stroke-linecap="round" stroke-linejoin="round" />
										</svg>
									</button>
								</div>
								<div class="p-5 sm:p-6">
									<h3 class="font-display text-lg sm:text-xl font-medium text-white leading-snug">
										RCK Community Voices – What the RCK Means to Us
									</h3>
								</div>
							</div>
						</div>
						{members.map((m) => {
							const bioText = m.description ? ricosToPlainText(m.description) : "";
							const hasBio = bioText.length > 0;
							const initials = m.familyName
								.split(/\s+/)
								.filter(Boolean)
								.map((w) => w.charAt(0).toUpperCase())
								.slice(0, 2)
								.join("");

							const photoMarkup = (
								<div
									class="card-photo relative aspect-square w-full overflow-hidden"
									style="background: linear-gradient(135deg, var(--color-navy-50) 0%, var(--color-navy-100) 50%, var(--color-gold-100) 100%);"
								>
									<div
										class="monogram-fallback absolute inset-0 flex flex-col items-center justify-center gap-2"
										style={m.photoUrl ? "display:none" : ""}
									>
										<span class="font-display text-6xl sm:text-7xl font-medium text-navy-200" aria-hidden="true">
											{initials || "RCK"}
										</span>
										<span class="gold-rule-sm opacity-60" aria-hidden="true"></span>
									</div>
									{m.photoUrl && (
										<img
											src={m.photoUrl}
											alt={m.familyName}
											loading="lazy"
											class="card-image h-full w-full object-cover"
											onerror="this.style.display='none';this.closest('.card-photo').querySelector('.monogram-fallback').style.display=''"
										/>
									)}
									{hasBio && (
										<div class="bio-overlay absolute inset-0 flex flex-col bg-gradient-to-b from-navy-700/80 via-navy-700/75 to-navy-700/70 p-5 sm:p-6">
											<div class="bio-scroll overflow-y-auto pr-1">
												<p class="text-[0.9375rem] leading-relaxed text-white whitespace-pre-line">
													{bioText}
												</p>
											</div>
										</div>
									)}
								</div>
							);

							const metaMarkup = (
								<div class="p-5 sm:p-6">
									<h3 class="font-display text-xl sm:text-2xl font-medium text-navy-600 truncate">
										{m.familyName}
									</h3>
									{m.hebrewName && (
										<p class="mt-1 text-sm sm:text-base text-navy-500 font-medium">
											{m.hebrewName}
										</p>
									)}
									{hasBio && (
										<span class="bio-toggle-label mt-3 inline-flex items-center gap-1 font-sans text-[11px] font-bold uppercase tracking-[0.22em] text-gold-500" aria-hidden="true"></span>
									)}
								</div>
							);

							const card = hasBio ? (
								<button
									type="button"
									data-community-card
									aria-expanded="false"
									class="community-card flex flex-col h-full w-full text-left rounded-md bg-white ring-1 ring-navy-100 overflow-hidden shadow-[0_12px_30px_-20px_rgba(15,23,42,0.25)] transition-all duration-300 hover:-translate-y-1 hover:shadow-[0_22px_45px_-22px_rgba(15,23,42,0.45)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gold-500 cursor-pointer"
								>
									{photoMarkup}
									{metaMarkup}
								</button>
							) : (
								<div class="flex flex-col h-full w-full rounded-md bg-white ring-1 ring-navy-100 overflow-hidden shadow-[0_12px_30px_-20px_rgba(15,23,42,0.25)]">
									{photoMarkup}
									{metaMarkup}
								</div>
							);

							return (
								<div class="w-[240px] sm:w-[270px] md:w-[290px] shrink-0 snap-start">
									{card}
								</div>
							);
						})}
						</div>

						<button
							type="button"
							data-members-prev
							aria-label="Previous member"
							class="absolute left-1 sm:-left-4 top-[38%] -translate-y-1/2 z-10 flex h-11 w-11 items-center justify-center rounded-sm bg-white/95 text-navy-600 ring-1 ring-navy-100 shadow-[0_10px_24px_-12px_rgba(7,23,58,0.4)] transition-colors hover:bg-gold-400"
						>
							<svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M15 6l-6 6 6 6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</button>
						<button
							type="button"
							data-members-next
							aria-label="Next member"
							class="absolute right-1 sm:-right-4 top-[38%] -translate-y-1/2 z-10 flex h-11 w-11 items-center justify-center rounded-sm bg-white/95 text-navy-600 ring-1 ring-navy-100 shadow-[0_10px_24px_-12px_rgba(7,23,58,0.4)] transition-colors hover:bg-gold-400"
						>
							<svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M9 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</button>
					</div>
				)}
			</div>
		</section>
		)}
	</main>
	<Footer />
</Layout>

<style>
	/* Same hover/tap-reveal mechanics as the team page's card (see design log
	   #007/#008), just relabeled "Read about us!" for this context. Kept as a
	   local copy rather than a shared component since the two cards diverge in
	   surrounding markup (role/eyebrow line here vs. there). */
	/* Member Spotlight is a native horizontal-scroll carousel, not a custom
	   swipe handler — overflow-x + scroll-snap already gives trackpad/mouse-wheel
	   scrolling and touch swipe for free. The arrow buttons below just nudge
	   scrollLeft by one card; the scrollbar itself is hidden for a cleaner look
	   since it's not the primary way anyone will actually scroll it. */
	.members-track {
		scrollbar-width: none;
	}
	.members-track::-webkit-scrollbar {
		display: none;
	}

	.card-image {
		transition: filter 400ms ease;
	}
	.bio-overlay {
		opacity: 0;
		pointer-events: none;
		transition: opacity 300ms ease;
	}

	@media (hover: hover) and (pointer: fine) {
		.community-card:hover .card-image {
			filter: grayscale(1);
		}
		.community-card:hover .bio-overlay {
			opacity: 1;
			pointer-events: auto;
		}
	}

	.community-card[aria-expanded="true"] .card-image {
		filter: grayscale(1);
	}
	.community-card[aria-expanded="true"] .bio-overlay {
		opacity: 1;
		pointer-events: auto;
	}

	.bio-toggle-label {
		display: none;
	}
	.bio-toggle-label::after {
		content: "Read about us!";
	}
	.community-card[aria-expanded="true"] .bio-toggle-label::after {
		content: "Hide";
	}
	@media (hover: none), (pointer: coarse) {
		.bio-toggle-label {
			display: inline-flex;
		}
	}

	.bio-scroll {
		scrollbar-width: thin;
		scrollbar-color: rgba(255, 255, 255, 0.35) transparent;
	}
	.bio-scroll::-webkit-scrollbar {
		width: 6px;
	}
	.bio-scroll::-webkit-scrollbar-thumb {
		background-color: rgba(255, 255, 255, 0.35);
		border-radius: 3px;
	}
</style>

<script>
	// Member Spotlight carousel: infinite loop. Clone the full card set (video
	// tile + family cards) once before and once after the real cards, so
	// there's always more (visually identical) content to scroll into in
	// either direction. Once scroll drifts a full "real set" width past
	// either edge, jump back by that width with no animation — since the
	// clone is pixel-identical to the real card at that position, the jump
	// is invisible. This runs before the community-card/video-spotlight
	// listener setup below so their querySelectorAll calls pick up the
	// clones too and wire them up identically to the originals. Skipped for
	// 4 or fewer cards (video tile included) — same reasoning as the events
	// page's flyer carousel: a short row doesn't need to loop.
	const LOOP_MIN = 4;
	document.querySelectorAll<HTMLElement>("[data-members-carousel]").forEach((carousel) => {
		const track = carousel.querySelector<HTMLElement>("[data-members-track]");
		if (!track) return;

		const originals = Array.from(track.children) as HTMLElement[];
		if (originals.length <= LOOP_MIN) return;

		const cloneSet = () =>
			originals.map((el) => {
				const clone = el.cloneNode(true) as HTMLElement;
				clone.setAttribute("aria-hidden", "true");
				clone.setAttribute("inert", "");
				return clone;
			});
		track.prepend(...cloneSet());
		track.append(...cloneSet());

		const firstReal = originals[0];
		const firstAfterClone = track.children[track.children.length - originals.length] as HTMLElement;
		const realWidth = firstAfterClone.getBoundingClientRect().left - firstReal.getBoundingClientRect().left;
		const startOffset = firstReal.getBoundingClientRect().left - track.getBoundingClientRect().left + track.scrollLeft;

		// The track has `scroll-smooth` (scroll-behavior: smooth) for the arrow
		// buttons' scrollBy(). A plain `track.scrollLeft = x` assignment inherits
		// that same smooth behavior per the CSSOM View spec, so the initial jump
		// to the real cards must use scrollTo({ behavior: "instant" }) to
		// override it.
		track.scrollTo({ left: startOffset, behavior: "instant" });

		// Only re-anchor once scrolling has fully settled — never mid-gesture.
		// Correcting while the track is still moving fights whatever's driving
		// it (native scroll/swipe momentum, or the arrow buttons' own smooth
		// scrollBy), which cancels/undoes that motion and shows up as stutter or
		// arrow clicks that seem to do nothing.
		//
		// Debounced on `scroll` rather than keyed off `scrollend`: after a wheel/
		// trackpad gesture ends, CSS scroll-snap still animates the track the
		// rest of the way to the nearest snap point, and that settle animation
		// keeps firing `scroll` events of its own — but in some browsers
		// `scrollend` fires before that settle finishes, so checking there reads
		// a not-yet-final position (needing a second scroll to catch up — the
		// "extra push"). A debounce that resets on every `scroll` tick instead
		// waits out that settle regardless of `scrollend` timing.
		//
		// EDGE_SLACK: startOffset/realWidth come from getBoundingClientRect
		// (sub-pixel floats), but the settled position is the browser's own
		// rounded pixel value — which can land a fraction of a px short of the
		// exact threshold. Without slack, that first crossing silently fails.
		const EDGE_SLACK = 4;
		const SETTLE_DELAY = 120;
		let settleTimer: ReturnType<typeof setTimeout> | undefined;
		track.addEventListener("scroll", () => {
			clearTimeout(settleTimer);
			settleTimer = setTimeout(() => {
				if (track.scrollLeft >= startOffset + realWidth - EDGE_SLACK) {
					track.scrollTo({ left: track.scrollLeft - realWidth, behavior: "instant" });
				} else if (track.scrollLeft <= startOffset - EDGE_SLACK) {
					track.scrollTo({ left: track.scrollLeft + realWidth, behavior: "instant" });
				}
			}, SETTLE_DELAY);
		}, { passive: true });
	});

	// Identical toggle logic to team.astro's card (see design log #007) —
	// hover reveals on desktop, tap-to-pin on touch, scroll resets when hidden.
	document.querySelectorAll<HTMLButtonElement>("[data-community-card]").forEach((card) => {
		const scrollEl = card.querySelector<HTMLElement>(".bio-scroll");
		let hovering = false;

		const resetScrollIfHidden = () => {
			const expanded = card.getAttribute("aria-expanded") === "true";
			if (!hovering && !expanded && scrollEl) scrollEl.scrollTop = 0;
		};

		card.addEventListener("mouseenter", () => { hovering = true; });
		card.addEventListener("mouseleave", () => {
			hovering = false;
			resetScrollIfHidden();
		});

		card.addEventListener("click", () => {
			const expanded = card.getAttribute("aria-expanded") === "true";
			card.setAttribute("aria-expanded", expanded ? "false" : "true");
			resetScrollIfHidden();
		});
	});

	// Video spotlight tile: the play button swaps the thumbnail for an inline
	// autoplaying embed in place, rather than jumping to YouTube or opening the
	// lightbox. The separate corner button (data-lightbox-open, wired by
	// Lightbox.astro's own delegated listener) still opens the same video
	// full-size for anyone who wants that instead.
	document.querySelectorAll<HTMLElement>("[data-video-spotlight]").forEach((tile) => {
		const playBtn = tile.querySelector<HTMLButtonElement>("[data-video-spotlight-play]");
		const thumb = tile.querySelector<HTMLImageElement>("[data-video-spotlight-thumb]");
		const scrim = tile.querySelector<HTMLElement>("[data-video-spotlight-scrim]");
		const videoId = tile.dataset.videoId;
		if (!playBtn || !thumb || !scrim || !videoId) return;

		playBtn.addEventListener("click", () => {
			const iframe = document.createElement("iframe");
			iframe.className = "absolute inset-0 h-full w-full";
			iframe.style.border = "0";
			iframe.title = "RCK Community Voices – What the RCK Means to Us";
			iframe.allow = "accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture; web-share";
			iframe.allowFullscreen = true;
			iframe.src = `https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&playsinline=1&rel=0`;
			thumb.replaceWith(iframe);
			scrim.remove();
			playBtn.remove();
		}, { once: true });
	});

	// Member Spotlight carousel: arrow buttons nudge native scroll by one card's
	// width. Trackpad/mouse-wheel scrolling and touch swipe already work with no
	// JS at all (plain overflow-x + scroll-snap) — this just adds a click target
	// for people who'd rather press a button than scroll/swipe. Arrows are never
	// disabled: with more than LOOP_MIN cards the carousel above loops
	// infinitely in both directions; at or below that count there's nothing to
	// loop, so an arrow at either end just stops scrolling, same as any plain
	// scroll container.
	document.querySelectorAll<HTMLElement>("[data-members-carousel]").forEach((carousel) => {
		const track = carousel.querySelector<HTMLElement>("[data-members-track]");
		const prevBtn = carousel.querySelector<HTMLButtonElement>("[data-members-prev]");
		const nextBtn = carousel.querySelector<HTMLButtonElement>("[data-members-next]");
		if (!track) return;

		const scrollByCard = (dir: 1 | -1) => {
			const card = track.firstElementChild as HTMLElement | null;
			const gap = parseFloat(getComputedStyle(track).columnGap) || 0;
			const delta = card ? card.getBoundingClientRect().width + gap : track.clientWidth * 0.8;
			track.scrollBy({ left: dir * delta, behavior: "smooth" });
		};

		prevBtn?.addEventListener("click", () => scrollByCard(-1));
		nextBtn?.addEventListener("click", () => scrollByCard(1));
	});
</script>
